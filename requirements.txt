# ── Core ──────────────────────────────────────────────────────────────────────
aiohttp>=3.9
aiofiles>=23.0
beautifulsoup4>=4.12
pymupdf>=1.24          # fitz — PDF extraction

# ── Google Drive ──────────────────────────────────────────────────────────────
google-api-python-client>=2.120
google-auth>=2.28
google-auth-oauthlib>=1.2

# ── ML / Training (install manually on Kaggle/Colab for GPU builds) ───────────
# torch>=2.2              # managed by Kaggle/Colab environment
# unsloth                 # pip install "unsloth[colab-new] @ git+https://..." (see docs)
# trl>=0.8
# peft>=0.10
# transformers>=4.40
# datasets>=2.19
# bitsandbytes>=0.43

# ── API / Trigger ─────────────────────────────────────────────────────────────
kaggle>=1.6
openai>=1.30           # optional, for LLM Q&A generation in prepare.py
anthropic>=0.25        # optional, for Claude as judge / code suggester
google-generativeai>=0.5 # optional, for Gemini

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.32        # dashboard/app.py

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66
python-dotenv>=1.0
pandas>=2.1            # for results analysis
pyyaml>=6.0            # eval/run_eval.py YAML parsing
jinja2>=3.1            # research_deliverables/ template rendering
